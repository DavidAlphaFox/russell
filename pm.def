1.01
  prop P,
  prop Q,
: prop (P -> Q),
  (P -> Q) = ((~ P) or Q),
.

=
  prop P,
: P = P,
.

==
  P = Q,
  Q = R,
: P = R,
.

=->=
  P = Q,
  R = S,
: (P -> R) = (Q -> S),
.

|-=
  P = Q,
  |- Q,
: |- P,
.

1.1
  |- P,
  |- (P -> Q),
: |- Q,
.

taut
  prop P,
: |- ((P or P) -> P),
.

add
  prop P,
  prop Q,
: |- (Q -> (P or Q)),
.

perm
  prop P,
  prop Q,
: |- ((P or Q) -> (Q or P)),
.

assoc
  prop P,
  prop Q,
  prop R,
: |- ((P or (Q or R)) -> (Q or (P or R))),
.

sum
  prop P,
  prop Q,
  prop R,
: |- ((Q -> R) -> ((P or Q) -> (P or R))),
.

~
  prop P,
: prop (~ P),
.

~=
  P = Q,
: (~ P) = (~ Q),
.

or
  prop P,
  prop Q,
: prop (P or Q),
.

=or=
  P = Q,
  R = S,
: (P or R) = (Q or S),
.

abs
  prop P,
: |- ((P -> (~ P)) -> (~ P)),
.

simp
  prop P,
  prop Q,
: |- (Q -> (P -> Q)),
.

transp~
  prop P,
  prop Q,
: |- ((P -> (~ Q)) -> (Q -> (~ P))),
.

comm
  prop P,
  prop Q,
  prop R,
: |- ((P -> (Q -> R)) -> (Q -> (P -> R))),
.
