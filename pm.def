0=0
  prop P,
: P = P.

=
  prop P,
  prop Q,
  P = Q,
: Q = P.

1.01
  prop P,
  prop Q,
  prop R,
  prop S,
  P = R,
  Q = S,
: (P -> Q) = ((~ R) or S).

->
  prop P,
  prop Q,
: prop (P -> Q).

=->=
  prop P,
  prop Q,
  prop R,
  prop S,
  P = Q,
  R = S,
: (P -> R) = (Q -> S).

|-=
  prop P,
  prop Q,
  P = Q,
  |- Q,
: |- P.

1.1
  prop P,
  prop Q,
  |- P,
  |- (P -> Q),
: |- Q.

taut
  prop P,
: |- ((P or P) -> P).

add
  prop P,
  prop Q,
: |- (Q -> (P or Q)).

perm
  prop P,
  prop Q,
: |- ((P or Q) -> (Q or P)).

assoc
  prop P,
  prop Q,
  prop R,
: |- ((P or (Q or R)) -> (Q or (P or R))).

sum
  prop P,
  prop Q,
  prop R,
: |- ((Q -> R) -> ((P or Q) -> (P or R))).

~
  prop P,
: prop (~ P).

~=
  prop P,
  prop Q,
  P = Q,
: (~ P) = (~ Q).

or
  prop P,
  prop Q,
: prop (P or Q).

=or=
  prop P,
  prop Q,
  prop R,
  prop S,
  P = Q,
  R = S,
: (P or R) = (Q or S).

2.01
  prop P,
: |- ((P -> (~ P)) -> (~ P)).

2.02
  prop P,
  prop Q,
: |- (Q -> (P -> Q)).

2.03
  prop P,
  prop Q,
: |- ((P -> (~ Q)) -> (Q -> (~ P))).

comm
  prop P,
  prop Q,
  prop R,
: |- ((P -> (Q -> R)) -> (Q -> (P -> R))).

2.05
  prop P,
  prop Q,
  prop R,
: |- ((Q -> R) -> ((P -> Q) -> (P -> R))).

2.06
  prop P,
  prop Q,
  prop R,
: |- ((P -> Q) -> ((Q -> R) -> (P -> R))).

2.07
  prop P,
: |- (P -> (P or P)).

2.08
  prop P,
: |- (P -> P).

2.1
  prop P,
: |- ((~ P) or P).

2.11
  prop P,
: |- (P or (~ P)).

2.12
  prop P,
: |- (P -> (~ (~ P))).

2.13
  prop P,
: |- (P or (~ (~ (~ P)))).

2.14
  prop P,
: |- ((~ (~ P)) -> P).

2.15
  prop P,
  prop Q,
: |- (((~ P) -> Q) -> ((~ Q) -> P)).

syll
  prop P,
  prop Q,
  prop R,
  |- (P -> Q),
  |- (Q -> R),
: |- (P -> R).

2.16
  prop P,
  prop Q,
: |- ((P -> Q) -> ((~ Q) -> (~ P))).

2.17
  prop P,
  prop Q,
: |- (((~ Q) -> (~ P)) -> (P -> Q)).

2.18
  prop P,
: |- (((~ P) -> P) -> P).

2.2
  prop P,
  prop Q,
: |- (P -> (P or Q)).

2.21
  prop P,
  prop Q,
: |- ((~ P) -> (P -> Q)).

2.24
  prop P,
  prop Q,
: |- (P -> ((~ P) -> Q)).

2.25
  prop P,
  prop Q,
: |- (P or ((P or Q) -> Q)).

2.26
  prop P,
  prop Q,
: |- ((~ P) or ((P -> Q) -> Q)).

2.27
  prop P,
  prop Q,
: |- (P -> ((P -> Q) -> Q)).

2.3
  prop P,
  prop Q,
  prop R,
: |- ((P or (Q or R)) -> (P or (R or Q))).

2.31
  prop P,
  prop Q,
  prop R,
: |- ((P or (Q or R)) -> ((P or Q) or R)).

2.32
  prop P,
  prop Q,
  prop R,
: |- (((P or Q) or R) -> (P or (Q or R))).

or2
  prop P,
  prop Q,
  prop R,
: prop (P or Q or R).

2.33
  prop P,
  prop Q,
  prop R,
  prop S,
  prop T,
  prop U,
  P = S,
  Q = T,
  R = U,
: (P or Q or R) = ((S or T) or U).
